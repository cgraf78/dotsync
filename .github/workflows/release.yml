name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: bash tests/dotsync-test

      - name: Create tarball
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          tar czf "dotsync-${VERSION}.tar.gz" \
            --transform "s,^,dotsync-${VERSION}/," \
            bin/ tests/ examples/ install.sh README.md LICENSE

      - name: Verify tarball
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          tmpdir=$(mktemp -d)
          tar xzf "dotsync-${VERSION}.tar.gz" -C "$tmpdir"
          bash "$tmpdir/dotsync-${VERSION}/tests/dotsync-test"
          rm -rf "$tmpdir"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: dotsync-*.tar.gz
          generate_release_notes: true
